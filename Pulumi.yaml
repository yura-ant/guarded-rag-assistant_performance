name: guarded-rag-assistant
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: .venv
description: Guarded Rag Assistant
config:
  pulumi:tags:
    value:
      pulumi:template: python
